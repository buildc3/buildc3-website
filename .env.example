# Neon DB — used by Netlify Functions only (never exposed to browser)
# Copy this file to .env and fill in your values
DATABASE_URL=postgresql://<user>:<password>@<host>/<db>?sslmode=require&channel_binding=require
